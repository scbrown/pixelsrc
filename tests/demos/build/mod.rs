//! Build System Demo Tests
//!
//! Demonstrates pxl.toml configuration parsing, validation, and build context setup.
//! Each test verifies a different aspect of the build system configuration.

mod basic_config;
mod build_variants;
mod incremental;
mod multi_target;
mod watch_mode;
